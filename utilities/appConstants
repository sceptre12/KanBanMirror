const CONNECTION_ESTABLISHED = "CONNECTION_ESTABLISHED"
const DEVICE_CONNECTED = 'DEVICE_CONNECTED'
const CLOSING_CONNECTION = "CLOSING_CONNECTION"

module.exports = {
    CONNECTION_ESTABLISHED,
    DEVICE_CONNECTED,
    CLOSING_CONNECTION
}